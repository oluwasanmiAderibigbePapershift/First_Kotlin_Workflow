<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstkotlinworkflow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/DesignTheme.DayNight"
        android:supportsRtl="true">
        <activity android:name=".validateForm.form.FormButtonActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".validateForm.ValidateFormActivity">

        </activity>
        <activity android:name=".validateAField.ValidateFieldActivity">

        </activity>
        <activity android:name=".addTwoNumbers.MainActivity" />
    </application>

</manifest>